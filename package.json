{
  "name": "safenode-inspecciones",
  "version": "1.0.0",
  "description": "Sistema de gestión de inspecciones preoperacionales SafeNode",
  "main": "safenode-inspecciones/src/server.js",
  "engines": { "node": "20.x" },
  "scripts": {
    "start": "node safenode-inspecciones/src/server.js",
    "dev": "node --watch safenode-inspecciones/src/server.js",
    "build:frontend": "cd frontend && npm install && npm run build",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}